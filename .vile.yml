vile:
  ignore:
    - appveyor.yml
    - coverage
    - coffeelint.json
    - .ackrc
    - .babelrc
    - .build
    - .npmignore
    - .travis.yml
    - .vim.custom
    - node_modules
    - tags
    - .test
    - .ctags
    - .git
    - circle.yml
    - .editorconfig
    - .eslintignore
    - .eslintrc
    - .gitignore
    - .npmrc
    - tags
    - tmp
    - tsconfig.json
    - tslint.json
    - tsd.json
    - vendor
    - README.md
    - typings
    - .vile.yml

depcheck:
  config:
    ignore_dev_deps: false
    ignore_dirs:
      - src
      - typings
      - node_modules
      - coverage
      - tmp
    ignore_deps:
      - bluebird
      - mocha
      - babel-cli
      - coffee-script
      - istanbul
      - release-it
      - typescript
      - wrench
      - pryjs
      - shelljs
      - "vile-**"

escomplex:
  config:
    use_maintainability_index: false
  ignore:
    - src

eslint:
  ignore:
    - lib

coverage:
  config:
    path: coverage/lcov.info

comment:
  ignore:
    - lib
